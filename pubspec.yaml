name: sea_chat
description: "A modern glassmorphism chat application powered by Supabase."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Backend
  supabase_flutter: ^2.8.4

  # Chat Features
  image_picker: ^1.1.2
  record: ^5.1.2
  just_audio: ^0.9.38
  permission_handler: ^11.3.1

  # Utilities
  uuid: ^4.5.1
  path_provider: ^2.1.5
  
  # Fonts
  google_fonts: ^6.2.1

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  image_picker: ^1.1.2
  record: ^5.2.1
  just_audio: ^0.9.46
  google_sign_in: ^6.2.1  # <-- ضيف هذا السطر
  flutter_launcher_icons: ^0.13.1
